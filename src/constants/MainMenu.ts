import PathName from "./PathName";

const MainMenu = [
    { key: "home", path: PathName.HOME },
    { key: "trips", path: PathName.TRIPS },
    { key: "invoice", path: PathName.INVOICE },
    { key: "setting", path: PathName.ORGANIZATION },
];

export default MainMenu;
